const Instructors = require('./instructors'),
      Students = require('./students'),
      Appointments = require('./appointments');

module.exports = {
    Instructors,
    Students,
    Appointments
}